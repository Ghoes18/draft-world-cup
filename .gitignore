# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Env (never commit secrets)
.env
.env.*
!.env.example
.env.local

# Test / coverage
coverage/
.vitest/

# Downloaded Fjelstul CSV cache (see data/fjelstul/ATTRIBUTION.md)
data/fjelstul/*.csv

# Zafronix API response cache (see import:zafronix)
data/zafronix/

# Transfermarkt API cache + dry-run reports (see import:transfermarkt-positions)
data/transfermarkt/

# Generated full catalog (run pnpm build:catalog)
data/catalog.json
apps/web/public/catalog.json

# Wikidata photo lookup cache (run pnpm import:photos)
data/player-photos.json

# Convex AI local install state (guidelines.md may be committed separately)
convex/_generated/ai/ai-files.state.json
apps/web/convex/_generated/ai/ai-files.state.json

# Cursor local hooks / editor state
.cursor/
